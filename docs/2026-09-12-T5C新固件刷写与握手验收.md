# 2026-09-12 T5C 新固件刷写与握手验收

## 写入范围

- 用户已明确授权只写 COM7 的 `0x20000` 应用分区。
- 写入镜像：`xiaozhi.bin`，2,858,928 bytes。
- 镜像 SHA256：`76A62A87B0392BB960F1DB61DACD4C5E372FEEE7CB8067A4B2E6744137B5AE08`。
- 实际擦写范围：`0x00020000–0x002d9fff`。
- NVS、otadata、分区表和 assets 未写入。

## 写后验证

- esptool 完成写入并报告 `Hash of data verified`。
- 刷前、刷后 NVS SHA256 均为 `FA0F90B335A135BBF454681E24F92A6319EA967210D6A57DCAD0125EEEA782D1`，配网数据保持不变。
- 启动日志确认 ESP-IDF `v6.0.2`、应用 `2.4.1`、8 MB PSRAM 和 16 MB Flash。
- 设备恢复原 Wi-Fi，取得局域网地址并访问 8723 OTA 接口。
- WebSocket handshake 成功并取得 session，进入持久 idle 控制通道。
- 正确设备 ID 为固件发送的 MAC 形态 ID；15 次状态采样全部为 `online / idle`，session 稳定，音频帧为 0。
- 空闲 15 秒后 output codec 关闭；观察窗口内未出现连接错误或重复告警，仅收到后台 keepalive。

## 边界

- hello/WebSocket/在线与静音门禁已通过。
- 实体唤醒词识别、用户听到 TTS、冲突中性播报和黄灯仍需现场发声与观察，不能由 API 或串口日志替代。

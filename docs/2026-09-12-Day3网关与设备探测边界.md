# 2026-09-12 Day 3 网关与设备探测边界

## 已验证

- 8731 `/health`、`/ready` 返回正常；8731 为 `127.0.0.1` 监听。
- 8723 `/health` 返回正常，设备和项目相关 OpenAPI 路由可发现。
- 受控串口只读探测确认 ESP32-S3 rev `v0.2`、8 MB PSRAM、16 MB Flash。
- 设备探针未写入 Flash；未执行刷写、NVS 备份或 OTA 恢复。
- 固件画像、ESP32 探针和产物检查：`29 passed`。

## 当前边界

- 8723 设备状态查询显示目标设备为 `offline`，没有真实 WebSocket hello 会话。
- 项目 query 和 conflict API 在当前运行上下文返回 `401 Unauthorized`，缺少有效项目授权，不能继续执行真实查询或冲突写入。
- 因此 Day 3 的真实链路“唤醒→第二句话→冲突播报→审核确认”仍为 `NOT_RUN`；本轮只完成网关路由探测和设备身份读取。

## 下一步唯一关键动作

在不改变数据库和不重放旧 token 的前提下，补齐当前用户授权并确认 8723/8731 进程配置一致，然后重新执行 Day 1 迁移后同步；同步成功后再进行 ESP32 刷写前 NVS/OTA 备份和实体闭环。

# 2026-09-12 Day 4 网关冷重启恢复

## 包装任务重启

- 重新启动 8731/8723 计划任务时，既有监听 PID 和设备 session 保持不变。
- 说明包装任务不会主动终止已经存活的固定子进程，服务保持连续。

## 8731 冷重启

- 停止计划任务后终止精确的 8731 监听 PID，再重新启动任务。
- 监听 PID 发生变化，证明新服务进程已创建。
- health/ready 恢复。
- 恢复后 `run-once` 返回 `completed / unchanged / healthy`，无 error_type。

## 8723 冷重启

- 停止计划任务后终止精确的 8723 监听 PID，再重新启动任务。
- 监听 PID 发生变化，health 恢复。
- 实体设备自动重连，WebSocket session 发生变化，最终为 `online / idle`。

## 数据与设备边界

- 未修改来源内容、active 决策、设备 Flash 或私有凭据。
- 冷重启后 active 决策保持 2，测试候选保持 0。
- 历史会议会话状态为 stopped/aborted；它不是当前同步或网关健康失败。

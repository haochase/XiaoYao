# 2026-09-12 Day 3 实体语音与审核闭环

## 实体语音链路

- 新固件设备保持 `online / idle`，使用同一 WebSocket session。
- 本机扬声器播放“你好小瑶”后，实体设备分别以 `0.530001`、`0.590399`、`0.547051`、`0.537747` 概率命中自定义唤醒词。
- 状态链可重复出现 `idle → connecting → listening → speaking → idle`。
- 两个查询主题均触发真实采音、上行和 TTS：终端方案、会前提醒默认提前多久。
- 冲突陈述“我们决定改用树莓派本地推理终端”触发 TTS，并在 8724 生成唯一 proposed 候选。
- 验收结束时设备 `online / idle`，累计接收 565 个音频帧。

## 审核闭环

- reject：候选进入 rejected，proposed 归零，active 决策数保持 2。
- accept：第二个候选进入 accepted，返回硬件决策 `version=2`。
- 为避免保留错误验收状态，使用同一受保护候选和 8724 人工审核路径恢复 ESP32-S3 方案，返回 `version=3`。
- 最终版本链：v1 superseded、验收 v2 superseded、恢复 v3 active。
- 最终 active 硬件决策恢复为 ESP32-S3，提醒策略仍为提前 10 分钟，无 proposed 候选。
- 同步刷新后仍为 `completed / unchanged / healthy`，人工批准版本未被来源同步静默覆盖。

## 物理证据边界

- 串口与状态 API 证明唤醒、采音、TTS 和候选/审核链路完成。
- 本轮未取得用户对“听到具体播报内容”和“肉眼看到黄灯”的确认；这两项不能由状态日志替代。

# 2026-09-12 当前对话开发进度总览

本文汇总本次对话中完成的开发、测试、真实验收、提交和发布准备工作。私有 Token、设备标识、局域网地址、Session ID、数据库和备份路径不写入本文。

## 一、涉及项目与分支

### Hui Anchor / XiaoYao

- 工作树：`E:\hackasons\MiniCPM_Ascend\.worktrees\hui-anchor`
- 分支：`chase/feat/hui-anchor`
- 本次运行状态更新前，本地分支与 `origin/chase/feat/hui-anchor` 均为 `afb97c9`。
- `b015c6f` T4 产物复核与 `afb97c9` 当前对话进度总览均已普通推送；未强推。

### 小千本地 PC 工程

- 工作树：`E:\haochase\agent-system`
- 分支：`xiaoqian-local-pc-flow`
- 远程与本地同步：`6bbb04b`
- 已完成本地 PC Mock 闭环和 20 轮基线。

## 二、Hui Anchor 今日完成内容

### Day 1：迁移后同步与 T4

- 恢复 8731/8723 监听、health/ready 和 DWS Core 信任门禁。
- 受控 `run-once` 多次返回 `completed / unchanged / healthy`。
- `failed_sources=0`、`error_type=null`、`pending=null`。
- 迁移后 active 决策保持两张：硬件选型、会前提醒。
- T4 三类产物用当前代码重新复核：project memory、pre-meeting、post-meeting 均通过 schema 和业务校验。
- 会后审核快照 4 条，状态分桶一致。

### Day 2：固件构建、备份与刷写

- 官方 ESP-IDF `v6.0.2` 构建成功，应用版本 `2.4.1`。
- 启用自定义唤醒词、VAD、持久控制通道和 `conflict_cue`。
- application SHA256：`76A62A87B0392BB960F1DB61DACD4C5E372FEEE7CB8067A4B2E6744137B5AE08`。
- merged image SHA256：`87660C682EF70FE87A900AD0DD65F29FA298F92F6BE394618FC66A7E3FE30395`。
- 刷写前完成分区表、NVS、otadata、ota_0 私有备份和 SHA 核对。
- 按授权只写应用分区；esptool 报告 `Hash of data verified`。
- 刷前/刷后 NVS SHA 一致，确认配网和身份数据未被清除。

### Day 3：实体语音与审核闭环

- 新固件启动、Wi-Fi、OTA、WebSocket hello 和持久 idle 控制通道通过。
- 多次真实唤醒链路通过：`idle → connecting → listening → speaking → idle`。
- 两个真实项目查询通过：终端方案、会前提醒默认提前多久。
- 冲突陈述生成 proposed 候选；reject 后 active 数保持 2。
- accept 产生硬件决策 v2；随后恢复 ESP32-S3 方案为 v3 active。
- 最终 active：硬件 ESP32-S3、提醒提前 10 分钟、proposed=0。
- 公开证据保留状态/API/串口边界；人耳播报和肉眼黄灯仍未由用户确认。

### Day 4：评测、负向、时钟和恢复

- gateway + tools 全量回归无失败。
- 负向矩阵覆盖 `http_error`、`sync_conflict`、pending 冲突、来源失效、失败 abort、租约清理。
- 时钟、会话、并发 tick、跨进程专项通过；环境相关 skip 保持明确。
- 8731 冷重启后新进程 health/ready 恢复，同步继续 healthy/unchanged。
- 8723 冷重启后新进程 health 恢复，实体设备自动重连并取得新 session。
- 10 场/30 组实体评测完成：
  - TP=8、FP=6、FN=7、TN=1
  - precision=57.14%
  - recall=53.33%
  - abstain=26.67%
- 因 precision 低于 80%，对外声明已收缩为“识别部分潜在冲突并创建待审核候选”。
- 20 次独立真实主线全部成功：
  - 中位数 13,739.79 ms
  - P95 19,491.45 ms
  - 最大 21,086.08 ms
  - TTS 音频帧合计 1,967
  - 模型调用次数：`NOT_OBSERVABLE`，运行时无公开 metrics

### Day 5：发布候选准备

- 建立发布候选清单、可恢复发布清单、证据矩阵、视频脚本和提交检查表。
- 三次完整排练通过：第 2、4、5 轮；失败轮次单独记录。
- 最终全量回归、`git diff --check`、服务健康、设备在线和数据库状态核对通过。
- 公开验收文档已移除现场 LAN 地址、串口名、设备标识和私有备份路径。
- Git 跟踪内容未发现私有 Token、manifest、数据库、备份二进制、固件产物或日志。

## 三、小千本地 PC 工程完成内容

- `scripts/local_pc_flow.py` 离线闭环通过：hello、heartbeat、status、音频分片、Mock ASR/Agent/TTS、设备状态和 metrics。
- 本地 PC 20 轮 Mock 基线：20/20 通过。
- 进程级中位数约 216.43 ms，P95 约 240.39 ms。
- 已提交并推送：`6bbb04b test(评测): 记录本地二十轮基线`。
- 明确边界：该基线不代表真实 MiMo、ESP32、麦克风、扬声器或无线时延。

## 四、今日主要提交

Hui Anchor / XiaoYao：

- `f6ea972` 固件构建与板型探测
- `5f38341` 网关与实机探测边界
- `c2ac9ee` Day 4 代码回归基线
- `fa27da9` Day 4 负向同步矩阵
- `dea1f31` Day 1 真实同步与刷写前门禁
- `77eb398` 新固件刷写与握手验收
- `f1e0d24` 实体语音与审核闭环
- `9c9879e` 十场三十组实体评测
- `9920969` Day 5 最终回归与验收边界
- `7db3922` 三次完整排练
- `7872c04` 视频与提交检查表
- `be0e978` 移除公开验收中的现场标识
- `3b4ee92` 可恢复发布清单
- `6605f71` 网关冷重启恢复
- `944d4de` 时钟会话与并发门禁
- `ab1b622` 真实主线长程边界
- `7bb5618` 二十次真实主线评测
- `577d51c` 五天收敛证据矩阵
- `b015c6f` T4 三产物复核
- `afb97c9` 当前对话开发进度总览

小千本地 PC 工程：

- `6bbb04b` 本地二十轮基线

## 五、当前运行状态

- 8731：health/ready 正常，loopback-only。
- 8723：health 正常，监听正常。
- 8724：已按当前 Windows 用户重新启动；`/`、`/api/summary`、`/api/conflicts`、`/api/session` 四个 GET 均为 200，真实浏览器显示 2 项有效决策、0 个待确认项、时钟正常。
- 实体设备：最近核对为 online/idle；设备自动重连已验证。
- 最新同步审计：`unchanged`，无错误。
- 稳定性心跳：已于 2026-09-12 19:00 后重新启用；从本次启用时刻重新计算，每 15 分钟只读检查一次，共 96 次。完成前不能判定 24 小时 PASS。

## 六、仍未完成或需要外部输入

1. 稳定性观察自然结束并取得完整 24 小时证据。
2. 用户确认是否实际听到播报内容。
3. 用户确认是否肉眼看到冲突黄灯。
4. 实际视频文件、公开链接、比赛表单和最终提交回执。

当前版本可以称为“工程与真实链路发布候选”，不能称为“最终比赛提交完成”。

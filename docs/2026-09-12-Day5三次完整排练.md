# 2026-09-12 Day 5 三次完整排练

## 通过轮次

第 2、4、5 轮满足完整排练门：

1. DWS `run-once` 返回 `completed / unchanged / healthy`。
2. 实体设备完成“终端方案是什么”的唤醒与采音。
3. 实体设备完成“会前提醒默认提前多久”的唤醒与采音。
4. 带唯一会议编号的冲突陈述生成一个 proposed 候选。
5. 候选通过 8724 reject，避免排练修改 active 决策。
6. 每轮结束均为 active 决策 2、proposed 候选 0。

## 重试与失败边界

- 每个语音步骤最多重试一次唤醒。
- 第 1 轮因提醒问题未采音，不计为完整排练。
- 第 3 轮因重复冲突陈述映射到既有 rejected 候选 ID，没有生成新 proposed 候选，不计为完整排练。
- 第 4、5 轮使用唯一会议编号避免候选幂等去重，均一次生成独立候选。

## 最终状态

- 设备 online / idle。
- 硬件决策 v3 active，内容为 ESP32-S3 语音终端方案。
- 会前提醒仍为提前 10 分钟。
- 没有 proposed 候选残留。
- 该排练证明同步、实体采音、项目查询、冲突候选和审核 reject 的可重复链路；用户人耳播报内容与肉眼黄灯仍需单独确认。
